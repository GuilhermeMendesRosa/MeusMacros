import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'br.com.roselabs',
  appName: 'meus-macros-front',
  webDir: 'dist/meus-macros-front/browser',
};

export default config;
